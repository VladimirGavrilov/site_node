// получаем форму
  let form = document.forms.myForm; // <form name="my"> element

  // получаем элемент
  let elem = form.elements.num; // <input name="one"> element

  alert(elem.value); 
  let newOption = new Option("Классика", "classic");
  
   